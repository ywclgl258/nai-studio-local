feat(mention): remove broken combobox UI, keep only textarea + /

The pose / character editors had:
  - 13x13 invisible "save" button
  - broken preset-combobox (no CSS = default browser rendering)
  - manage button
  - kind badge

This created 4 dead-looking elements that confused users about what
"preset selection" means. With mention-popup, the textarea `/` trigger
is the new primary path.

Replaced all that with a single clean toolbar row:
  - tip: "在文本里输 / 弹出预设候选" (with kbd-styled /)
  - save button (bigger, 14x14 icon + "保存当前" text)

Net result: cleaner UI, single mental model (textarea = input, /
= insert preset).
